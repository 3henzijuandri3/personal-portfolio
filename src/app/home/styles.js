const styles = {
    container: 'container flex items-center h-screen bg-white lg:max-w-[768px] xl:max-w-[1140px]',
    homeTextContainer: 'flex flex-col items-start',
    textName: 'font-bold text-primary text-[40px] leading-none',
    textDescription: 'max-w-3xl font-mono text-[14px] text-secondary mt-[16px] leading-6 text-justify',
}

export default styles;